import DefaultLayout from "../components/DefaultLayout";

const About = props => (
  <DefaultLayout>
    <h1>About</h1>
    <p>
      LumberJack is a simple system to let you easily track data about yours
      app(s).
    </p>
  </DefaultLayout>
);

export default About;
